'use strict';

const compression = require('./compression');
const CONST = require('./const');
const isVote = require('./isVote');
const put = require('./put');

module.exports = {
  compression,
  CONST,
  isVote,
  put,
};
