dynamodbWriter:
  Effect: Allow
  Action:
    - dynamodb:UpdateItem
  Resource:
    Fn::ImportValue: ${self:custom.namespace}-table-arn

xRayWriter:
  Effect: Allow
  Action:
    - xray:PutTraceSegments
    - xray:PutTelemetryRecords
  Resource: '*'
